server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/estudy_plus?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: estudy123
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# JWT 配置
estudy:
  jwt:
    secret: eSt1ZHlQbHVzU2VjcmV0S2V5Rm9ySldUVG9rZW4yMDIz
    expiration: 86400000  # 24小时，单位毫秒

# 钉钉配置
dingtalk:
  app-key: ding_xxxxxxx
  app-secret: xxxxxx
  agent-id: 123456789
  callback-url: http://localhost:8080/api/dingtalk/callback
